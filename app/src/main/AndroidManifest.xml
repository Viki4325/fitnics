<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.group12.fitnics">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fitnics">
        <activity
            android:name=".presentation.IndividualFoodActivity"
            android:theme="@style/Theme.Fitnics.NoActionBar" />
        <activity
            android:name=".presentation.IndividualExerciseLogActivity"
            android:theme="@style/Theme.Fitnics.NoActionBar" />
        <activity android:name=".presentation.UpdateUserInfoActivity" />
        <activity android:name=".presentation.ExerciseLogActivity" />
        <activity android:name=".presentation.SearchExerciseActivity" />
        <activity
            android:name=".presentation.IndividualExercise"
            android:theme="@style/Theme.Fitnics.NoActionBar" />
        <activity
            android:name=".presentation.HomeActivity"
            android:theme="@style/Theme.Fitnics.NoActionBar" />
        <activity android:name=".presentation.SignUpActiveLevelActivity" />
        <activity android:name=".presentation.SignUpInfoActivity" />
        <activity android:name=".presentation.SearchFoodActivity" />
        <activity android:name=".presentation.FoodLogActivity" />
        <activity android:name=".presentation.SignUpActivity" />
        <activity android:name=".presentation.LogInActivity" />
        <activity android:name=".presentation.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>